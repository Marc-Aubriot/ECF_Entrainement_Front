import React from 'react';
import Logo from './Logo.js';
import Nav from './Nav.js';

const Header = () => {

    return (
        <header>
            <Logo />
            <Nav />
        </header>
    );
}

export default Header;